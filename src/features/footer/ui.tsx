import { Box } from '@mui/material';

import { RunningLineNews } from '../running-line-news';

export const Footer = () => {
  return (
    <Box paddingTop="20px">
      <RunningLineNews />
    </Box>
  );
};
